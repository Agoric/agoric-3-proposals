---
'@agoric/synthetic-chain': patch
---

Update `@agoric/synthetic-chain` dependencies to newer patch/minor releases, including `ts-blank-space`, `better-sqlite3`, `chalk`, `execa`, `tsup`, and `typescript`.
